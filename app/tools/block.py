
class BlockWayTool:
    """Manages the state for the block way tool."""
    def __init__(self):
        # This tool might not need specific settings like weight for now.
        # It simply marks edges as impassable.
        print("BlockWayTool initialized.")

    # Add methods if specific logic is needed later
    # For example, defining how the block is applied (e.g., line vs. node click)
